#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b;

    scanf("%d",&a);
    b=printf("%d",a);
    if(a>=0)
        printf("%d",b);
    if(a<0)
        printf("%d",b);
        
        
    
        
    return 0;
}
