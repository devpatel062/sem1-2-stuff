#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,c;
    c=0;
    scanf("%d",&a);
    if(a%5==0 || a%7==0)
        printf("%d",a);
    
    
    
    
        
    
        
    
    if(a%5!=0 && a%7!=0)
        printf("%d",c);
    
    return 0;
}
